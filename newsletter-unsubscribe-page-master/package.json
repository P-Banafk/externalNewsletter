{
  "name": "newsletter_unsubscribe",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd \"frontend\" && npm install && npm run build",
    "dev": "npm install && npm run build && nodemon index.js",
    "start": "npm install && npm run build && pm2 start index.js --name newsletter-unsubscribe && pm2 save"
  },
  "keywords": [],
  "author": "DC",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "mongoose": "^8.16.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
